import React, { useState } from "react";
import styled from "styled-components";

function PlayerList() {
  const [show, setShow] = useState(true);

  return <PlayerListWrapper></PlayerListWrapper>;
}

export default PlayerList;
